# Vorbedingungen

Damit Meldungen importiert werden können, benötigt man ein entsprechendes XML, welches aus GymNet exportiert werden muss.&#x20;

Des weiteren müssen die Anmeldedaten für die TurnFix Datenbank bekannt sein, oder es muss TurnFix auf dem PC installiert sein.&#x20;
