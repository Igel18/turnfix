# Leistung erfassen rot hinterlegt

Beim erfassen einer Leistung wird diese rot hinterlegt.&#x20;

Dies liegt daran das eine Validierung eingestellt ist und der eingegebenen Wert nicht valide ist. Die Validierung kann an der Disziplin am Wettkampf geändert werden.&#x20;
