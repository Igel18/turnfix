{
  "name": "@turnfix/shared",
  "version": "1.0.0",
  "description": "Shared utilities for TurnFix (client, server, jury-portal)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "expr-eval": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.35",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
